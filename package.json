{
  "name": "dima-plugin-firefox",
  "version": "2.1.1",
  "private": true,
  "description": "Plugin Firefox DIMA (Digital Influence Manipulation Analyzer) — M82 Project",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "audit": "npm audit --audit-level=high",
    "ci": "npm run lint && npm test && npm run audit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "happy-dom": "^20.14.0",
    "vitest": "^5.0.0"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
